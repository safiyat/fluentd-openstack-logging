# TIMESTAMP {date time} LEVEL  API   [] MESSAGE

#Logs sometimes contain https://github.com/openstack/openstack-manuals/blob/master/tools/autogenerate-config-flagmappings/cinder.flagmappings
<source>
    type tail
    path /opt/stack/logs/c-vol.log

    format grok
    grok_pattern %{CINDERVOL}
    custom_pattern_path /home/safiyat/GitHub/fluentd-openstack-logging/GrokPatterns/pattern

    tag cinder.volume
</source>

<filter cinder.volume>
    type record_transformer
    enable_ruby
    <record>
        host ${hostname}
        # component ${tag_parts[0]}
        subcomponent ${tag_parts[1]}
        request ${request.gsub!("\u001b".encode('utf-8'), "");request.gsub(/\[0[0-2](;[0-9]{2})?m/, '')}
        message ${message.gsub!("\u001b".encode('utf-8'), "");message.gsub(/\[0[0-2](;[0-9]{2})?m/, '')}
        timestamp ${timestamp.gsub!(" ", "T");timestamp.gsub(/\.\d*/, "+05:30")}      # 2015-11-04T13:54:44+05:30
    </record>
</filter>

<match cinder.volume>
    type retag
    add_prefix output.storage
</match>

###############################################################################
###############################################################################
###############################################################################

# <match output.**>
#     type stdout
# </match>
